[project]
name = "halberd-ot"
version = "0.1.0"
description = "HALBERD: Happened-Before Analytics & Logic Baseline for Edge Response & Defense"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.5.0",
    "pyyaml>=6.0.0",
    "networkx>=3.0",
    "numpy>=1.26.0",
    "websockets>=12.0",
]

[tool.vercel]
entrypoint = "seal.edgereactor.app:app"

